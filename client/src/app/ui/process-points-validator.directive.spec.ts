import { ProcessPointsValidatorDirective } from './process-points-validator.directive';

describe('MaxValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcessPointsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
